#!/bin/bash

# I assume you
# 1. following the README file
# 2. build phoenix image yourself and tag it gimi/phoenix:1.0.2

sudo docker run -it --rm --name phoenix -v "${PWD}:/code" --link rethinkdb211:rethinkdb --expose 4000 -p "4000:4000" gimi/phoenix:1.0.2 mix phoenix.server
